---
description: 项目全局规范（语言、安全、响应式、目录结构）
alwaysApply: true
---

# 项目全局规范

后续在本仓库内的 **规划、代码、注释、用户可见文案、提交说明与 AI 回复** 均须遵守下列约定。

## 1. 语言规则

- **统一使用简体中文**：文档、界面文案、错误提示、日志说明（面向开发者时）、注释中的自然语言说明等。
- 专有名词、API 名、库名、代码标识符保持英文原样；必要时可在括号内附中文说明。

## 2. 安全规则（数据库与 SQL）

- **禁止**将用户输入或不可信数据通过字符串拼接进 SQL 语句。
- **必须**使用各数据访问层提供的参数化查询 / 绑定参数（如 SQLAlchemy 的绑定参数、`cursor.execute(sql, params)`、`?`/`%s` 占位符等，按所用栈为准）。
- 动态表名、列名若不可避免，须使用 **严格白名单** 映射为固定标识，不得直接拼接用户字符串。

## 3. 响应式布局

- 前端界面 **必须** 同时适配 **PC 与移动端浏览器**（常见断点下可读、可点、无横向溢出失控）。
- 新建页面与组件时默认按响应式实现；不得假设仅桌面端。

## 4. 项目结构

- 新建或扩展代码库时，遵循 **当前技术栈通行目录约定**（例如前后端分目录、分层清晰、配置与环境分离、静态资源与源码分离）。
- 在本仓库内新增模块时，与已有结构保持一致；无先例时采用 **该栈社区常见的标准结构**，避免随意堆叠。

## 5. Git 规则
- 🚫 **严禁自动提交**：必须用户确认后才能执行 `git add/commit`
- 提交前展示 `git status` 和 `git diff`
- 格式：`<type>: <中文描述>`（feat/fix/docs/style/refactor/test/chore/ci）
- 🚫 禁止 AI 署名（No Claude/AI attribution）